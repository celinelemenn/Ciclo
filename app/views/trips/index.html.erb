<%= render 'shared/topmenu' %>

<div class="uni-page-container">
  <h2 class="mb-5 mx-3">Trips from Cyclo-ers by routes</h2>

<!-- this is the search part
  <div class="mb-50 search">
    <%= form_tag trips_path, class: "form-container", method: :get do %>
    <%= text_field_tag :query,
      params[:query],
      class: "form-control form-input",
      placeholder: "Filter by keywords"
    %>
    <%= submit_tag "Search", class: "btn-search" %>
    <% end %>
  </div>
 -->

<!-- this is the results listing all trips -->
  <div class="mb-7rem">
    <% @cycling_routes.each do |cycling_route| %>

    <% unless @trips.where(cycling_route: cycling_route).empty? %>

      <h5 class="ml-3 mt-5 mb-3"><%= cycling_route %></h5>
      <% @trips.where(cycling_route: cycling_route).each do|trip|   %>
        <%= render 'shared/trips_card', trip: trip %>
      <% end %>
    <% end %>

    <% end %>

    <h5 class="ml-3 mt-5 mb-3">No Specific Cycling Route</h5>
    <% @trips_noroute.each do |trip| %>
      <%= render 'shared/trips_card', trip: trip %>
    <% end %>

  </div>

  <div>

  </div>
</div>
<%= render 'shared/navbar' %>
