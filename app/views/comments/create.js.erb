function refreshForm(innerHTML) {
  const newCommentForm = document.getElementById('new_comment');
  newCommentForm.innerHTML = innerHTML;
}

function addComment(commentHTML) {
  console.log('djfhsdkfhsdjkh')
  const comments = document.getElementById('comments');
  comments.insertAdjacentHTML('beforeend', commentHTML);
}

<% if @comment.errors.any? %>
  refreshForm('<%= j render "comments/form", point_of_interest: @poi, comment: @comment %>');
<% else %>
  addComment('<%= j render "comments/show", comment: @comment %>');
  refreshForm('<%= j render "point_of_interests/show", point_of_interest: @poi, comment: Comment.new %>');
<% end %>
