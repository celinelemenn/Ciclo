<div>
  <p>Chatting with <%= conversation.with(current_user.name) %></p>
  <br>
  <p><em><%= conversation.personal_messages.last.body.truncate(50) %></em></p>
  <br>
  <%= link_to 'View conversation', conversation_path(conversation) %>
  <hr>
</div>
